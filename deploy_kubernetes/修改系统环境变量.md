```
yum -y install epel-release vim unzip gcc gcc-c++ lrzsz autoconf automake make zlib zlib-devel openssl openssl-devel pcre pcre-devel maven
```

```
yum -y install git python36 wget java java-devel iftop net-tools sysstat iotop GeoIP GeoIP-devel perl-Geo-IP python3-devel docker-compose
```